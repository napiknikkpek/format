#include <boost/hana/string.hpp>

#include "format.hpp"

using namespace boost::hana::literals;

int main() {
<% 2.times do |k| %>

#if defined(METABENCH)
  format(
      "<%= '{}asdfsdfsadfasdfsadfasdfasdfasdfsadfsa' * n %>"_s, 
      <%= (1..n).to_a.join(',') %>
  );
#endif
<% end %>

  return 0;
}
