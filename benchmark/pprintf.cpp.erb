#include <pprintpp.hpp>

#include <cstdio>

int main()
{
<% 2.times do |k| %>

#if defined(METABENCH)
  pprintf(
      "<%= '{}asdfsdfsadfasdfsadfasdfasdfasdfsadfsa' * n %>", 
      <%= (1..n).to_a.join(',') %>
  );
#endif
<% end %>

  return 0;
}
