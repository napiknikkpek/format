#include <sstream>

#include "format.hpp"

#include <boost/hana/string.hpp>

int main() {
  using namespace boost::hana::literals;

  std::stringstream ss;

#if defined(METABENCH)
  ss << format("<%= env[:templ] * (n + 1) %>"_s, 0);
#endif

  return 0;
}
